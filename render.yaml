services:
  - type: web
    name: uc-dfnet-steganalysis
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_web:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
